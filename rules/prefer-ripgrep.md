---
description: Prefer the first-class ripgrep tool for repository content searches.
alwaysApply: true
---

For ordinary repository content searches, use the `rg` tool instead of `grep`. Use `glob` for filename discovery. Use `grep` only when its internal-URL, archive, selector, or remote-path support is specifically needed.
